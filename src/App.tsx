import React from "react";
import { Navigation } from "./components/navigation";

// Gives us the chance to wrap our application in Providers if needed.
const App = () => (
  <>
    <Navigation />
  </>
);

export default App;
